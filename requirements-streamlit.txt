# =============================================================================
# STREAMLIT CLOUD ULTRA-FAST DEPLOYMENT
# Minimal requirements for fastest possible deployment
# =============================================================================

# Core Streamlit (keep pinned for stability)
streamlit==1.32.0

# Essential data processing (use ranges for pre-compiled wheels)
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0

# Core visualization
plotly>=5.15.0,<6.0.0

# Essential utilities
python-dateutil>=2.8.0,<3.0.0
pytz>=2023.0,<2025.0

# =============================================================================
# DEPLOYMENT STRATEGY
# =============================================================================
# This file uses minimal dependencies to achieve fastest deployment
# Additional features can be added after initial deployment
# Expected deployment time: 1-3 minutes instead of 15+ minutes
# ============================================================================= 